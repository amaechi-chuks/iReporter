import incidentRouter from './incidentRoutes';
import user from './user';
import defaultRoute from "./defaultRoute";

const router = {
    incidentRouter, user, defaultRoute
    };

export default router;